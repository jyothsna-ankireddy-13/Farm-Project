import LayoutBar from "../../components/layout";
import SettingsContainer from "../../Container/settings";
const Settings = () => {
  return (
    <LayoutBar title="Settings">
      <SettingsContainer/>
    </LayoutBar>
  );
};

export default Settings;